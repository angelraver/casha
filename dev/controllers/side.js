class SideController {
  constructor($scope) {
    'ngInject';
  }
}

let SideModule = angular.module('SideModule', []);
SideModule.controller('SideController', SideController);

export default SideModule = SideModule.name
